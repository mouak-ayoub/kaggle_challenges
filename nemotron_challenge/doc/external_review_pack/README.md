# External Review Pack

This folder contains handoffs for an external reviewer such as ChatGPT Pro.

## What To Send Now

Use the second-round status brief:

```text
doc/external_review_pack/ROUND2_STATUS_REVIEW_BRIEF.md
```

Optional attachments:

```text
data/outputs/reports/experiment_dashboard.html
data/analysis/cipher_public_test_00189f6a_trace_comparison.md
```

The round-2 brief summarizes the latest checkpoint-144 public score (`0.59`), checkpoint-96's `0.58` near-neighbor result, the hard-family failures, and the current cipher-trace diagnosis. It asks the reviewer for a concrete next experiment recommendation.

## Historical Context

The first neutral review brief is still available:

```text
doc/external_review_pack/CHATGPT_PRO_REVIEW_BRIEF.md
```

The response/synthesis from the first review is:

```text
doc/external_review_pack/GLOBAL_REVIEW_RESPONSE.md
```

Use these only if the reviewer needs the full earlier context.

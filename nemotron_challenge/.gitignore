# Local data, generated artifacts, and external references
/data/

# Legacy/root artifact dirs; keep ignored if accidentally recreated.
/outputs/
/models/
/wandb/

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# Environments
.venv/
venv/

# Notebooks
.ipynb_checkpoints/

# Kaggle credentials should stay outside the repo
kaggle.json

# Local Kaggle kernel/reference pulls should live under data/reference/.
# Keep root temp pulls ignored if the Kaggle CLI recreates them.
.kaggle_tmp*/
/build/
